from matcher import Matcher
from textacy.keyterms import sgrank

matcher = Matcher(url='https://en.wikipedia.org/wiki/Support_vector_machine')

matcher.preprocess()
matcher.extract_keyphrases(algorithm=sgrank, n=20, ngrams=(2, 3))

print(matcher.keyphrases)